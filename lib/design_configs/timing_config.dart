class TimingConfig {
  static const Duration scanResultDelay = Duration(seconds: 3);
  static const Duration autoResetDelay = Duration(seconds: 5);
  static const Duration errorDisplayDuration = Duration(seconds: 3);
}
